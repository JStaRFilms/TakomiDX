export function createRouteRegistryBoundary() {
  return {
    name: "route-registry",
    note: "Reserved for stable preview and auth route registration.",
    status: "placeholder",
  } as const;
}
